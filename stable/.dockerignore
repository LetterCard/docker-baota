# ==============================================================================
#  构建上下文排除清单
#
#  作用有两个：
#    1) 减小上下文体积，构建更快
#    2) 防止把「只在 CI / 本地使用」的东西打包进生产镜像
#
#  Dockerfile 目前只 COPY 三个运行期脚本（init-mounts / entrypoint / patch-panel），
#  下面的排除是一道额外防线 —— 万一以后有人图省事写成 COPY scripts/ ，
#  测试脚本也不会被带进镜像。
# ==============================================================================

# 版本控制
.git
.gitignore

# 运行期数据与本地产物
data/
.verify/
*.log

# 项目自身文件，镜像内不需要
Dockerfile
docker-compose.yml
*.md
VERSION

# CI 专用脚本，绝不进生产镜像（它在 runner 上跑 docker 命令，容器内没有意义）
scripts/health-check.sh
